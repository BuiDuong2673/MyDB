# Environment & secrets
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.py[cod]

# Dependencies (never commit — includes large platform binaries e.g. @next/swc-*)
node_modules/

# Next.js build output
.next/
out/

# OS / editor
.DS_Store
.idea/
.vscode/

# Debug / logs
npm-debug.log*
pnpm-debug.log*
*.log

# Convex AI / Cursor agent skills (optional; reinstall with `npx convex ai-files install`)
# Not required for `npm run build`, deploy, or runtime — safe to omit from GitHub.
.agents/
skills-lock.json
AGENTS.md
CLAUDE.md
convex/_generated/ai/
