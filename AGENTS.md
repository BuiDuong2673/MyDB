# AGENTS.md

## Cursor Cloud specific instructions

### Project overview

MyDB is described as "The AI travel advisor that finds the perfect train for your journey." The repository is currently a scaffold with no source code or dependencies — only `README.md`, `.gitignore`, and `LICENSE`.

### Tech stack

- **Language:** Python (inferred from `.gitignore`)
- **Python version available:** 3.12.3 (system)

### Development environment

- Python 3.12 is pre-installed on the VM. No additional system dependencies are needed at this time.
- There are no dependency files (`requirements.txt`, `pyproject.toml`, etc.) yet. When they are added, the update script should be updated accordingly.
- No services to start, no linters to run, no tests to execute, and no build steps until source code is added.
