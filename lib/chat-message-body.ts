/**
 * Typography shared by the composer (`ChatInput`) and every message bubble so
 * pasted templates, new chats, and sidebar history look identical.
 */
export const CHAT_MESSAGE_BODY_TYPOGRAPHY =
  "text-sm leading-relaxed text-foreground font-sans [overflow-wrap:anywhere] whitespace-pre-wrap";
